doctype html
html(lang='es')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title Login - Panel de Admin
    link(rel='stylesheet', href='/css/admin.css')
  body.login-page
    .login-container
      .login-card
        .login-header
          h1 🐧 Panel de Admin
          p.subtitle Tienda de Larry el Pingüino
        
        if error
          .alert.alert-error= error
        
        form.login-form(method='POST', action='/auth/login')
          .form-group
            label(for='username') Usuario
            input#username(type='text', name='username', required, placeholder='paula', autofocus)
          
          .form-group
            label(for='password') Contraseña
            input#password(type='password', name='password', required, placeholder='••••••••')
          
          button.btn.btn-primary(type='submit') Iniciar Sesión
        
        .login-footer
          p.hint 💡 Usuario por defecto: 
            strong paula
            |  / 
            strong pescado123
          p.hint Si no existe, 
            a(href='/auth/create-initial-user') créalo aquí
