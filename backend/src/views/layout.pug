doctype html
html(lang='es')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title= title || 'Panel de Admin - Larry el Pingüino'
    link(rel='stylesheet', href='/css/admin.css')
  body
    if user
      nav.navbar
        .nav-container
          h1.logo 🐧 Panel de Paula
          .nav-links
            a(href='/products') Productos
            a(href='/orders') Pedidos
            .user-info
              span Hola, #{user.username}!
              a.logout-btn(href='/auth/logout') Cerrar Sesión
    
    main.container
      block content
    
    footer.footer
      p © 2024 Larry el Pingüino - Tienda Online Antártica 🧊
